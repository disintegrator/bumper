[settings]
pin = true

[env]
# hk's embedded pkl evaluator (pklr) can't parse JSON (`json.Parser`), which
# hk.pkl relies on to source oxfmt excludes from .oxfmtrc.json. Use the real
# pkl CLI instead.
HK_PKL_BACKEND = "pkl"

[tools]
bun = "1.3.14"
go = "1.26.5"
hk = "1.53.0"
pkl = "0.31.1"
